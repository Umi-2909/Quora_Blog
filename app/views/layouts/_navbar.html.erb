<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <style>
      ul li {
          list-style: none;
      }

      a {
          text-decoration: none;
      }

      a:hover {
        color: black;
      }

      .menu {
          position: fixed;
          top: 0;
          width: 100%;
          height: 80px;
          background-color: #185886;
          padding: 0 50px;
          display: flex;
          justify-content: space-between;
          align-items: center;
          transition: 0.4s;
          z-index: 100;
      }

      .menu__list {
          display: flex;
      }

      .menu__item {
      }

      .menu__link {
          position: relative;
          overflow: hidden;
          display: flex;
          flex-direction: column;
          justify-content: center;
          align-items: center;
          color: #fff;
          padding: 5px 20px;
          font-size: 1.1em;
          font-weight: 700;
      }

      .brand {
          position: relative;
          overflow: hidden;
          font-size: 1.8em;
          font-weight: 700;
          color: #fff;
          padding: 5px 0;
      }
    </style>
  </head>
  <body>
    <nav class="menu" id="navbar">
      <%= link_to "Blogger",home_path,class:'brand'%>
      <ul class="menu__list">
        <li class="menu__item" style="--i:0;">
          <%= link_to "Diễn đàn",posts_path,class:'menu__link' %>
        </li>
        <li class="menu__item" style="--i:0;">
          <%= link_to "Thành viên",users_path,class:'menu__link' %>
        </li>
        <li class="menu__item" style="--i:0;">
          <%= link_to "Thông tin",about_path,class:'menu__link' %>
        </li>
      </ul>
      <ul class="menu__list">
        <li><%= render 'search/form' %></li>
        <li><%= render 'user/session_manager' %></li>
      </ul>
    </nav>
  </body>
</html>
